#pragma once
#define STUFF_HPP_

//Takes a real number and returns a number between 0 to 1
double f(double x);
//Takes a number between 0 to 1 and turns it into a real number
double g(double x);

//g = f^-1
//f = g^-1